<%- include('../partials/header') %>

<div class="container py-4">
    <h2 class="fw-bold mb-4">Mi Agenda de Trabajo</h2>
    
    <div class="card border-0 shadow-sm mb-4">
        <div class="card-body">
            <label class="form-label fw-bold">Ver citas para el día:</label>
            <input type="date" id="fechaFiltro" class="form-control w-auto d-inline-block ms-2" value="<%= new Date().toISOString().split('T')[0] %>">
        </div>
    </div>

    <div class="table-responsive bg-white rounded shadow-sm">
        <table class="table table-hover align-middle mb-0">
            <thead class="table-dark">
                <tr>
                    <th>Hora</th>
                    <th>Cliente</th>
                    <th>Servicio</th>
                    <th>Estado</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody id="tabla-agenda">
                </tbody>
        </table>
    </div>
</div>

<script src="/js/staff_agenda.js"></script>
<%- include('../partials/footer') %>